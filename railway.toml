[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "bash -c \"python init_db.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT\""
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
