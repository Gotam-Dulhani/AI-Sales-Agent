[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "bash -c 'python init_db.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT 2>&1'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
